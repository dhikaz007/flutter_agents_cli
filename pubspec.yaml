name: flutter_agents_cli
description: Dynamic, project-aware, token-efficient AGENTS.md initializer and rule manager for Flutter projects.
version: 2.1.3
publish_to: none
environment:
  sdk: ">=3.3.0 <4.0.0"
dependencies:
  args: ^2.7.0
  crypto: ^3.0.6
  path: ^1.9.1
  yaml: ^3.1.3
dev_dependencies:
  test: ^1.25.15
executables:
  agents: agents
