# Architecture Profile — Custom Existing

The existing project structure is the source of truth.

- Inspect neighboring features before creating folders.
- Preserve established roots, naming, layers, and dependency direction.
- Do not migrate to another architecture profile during unrelated work.
- If the structure is inconsistent, make the smallest local change and report architecture debt rather than silently reorganizing the project.
