# Loading Profile — loader_overlay

Use blocking overlay loading only for operations where user interaction must be prevented, such as critical authentication/submission flows. Show/hide it from UI/listener according to state; do not control widget UI from business/state classes.
