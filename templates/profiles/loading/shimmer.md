# Loading Profile — shimmer

Use for small inline/text placeholders when that is the project's established convention. Avoid using shimmer as a blocking full-screen loader or when a stable skeleton component already exists.
