# Loading Profile — skeletonizer

Use for initial list/card/content-shape loading where preserving layout reduces visual shift. Prefer skeletons matching final content geometry. Refresh/next-page flows should usually keep existing content visible rather than replacing it with a full skeleton.
