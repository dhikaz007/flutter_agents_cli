# Storage Profile — Drift

- Reuse the existing database, table, DAO/repository, migration, and isolate strategy.
- Schema changes require an explicit migration path and compatibility review.
- Keep raw database concerns below presentation/state layers.
