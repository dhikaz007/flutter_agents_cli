# Storage Profile — shared_preferences

Use only for small non-sensitive preferences consistent with the existing project. Do not use it for credentials/tokens or as a replacement for structured persistence without an explicit decision.
